server:
  port: 8888

spring:
  profiles:
    active: native
  application:
    name: configServer
  cloud:
    config:
      server:
        native:
          search-location: classpath:/configuration

